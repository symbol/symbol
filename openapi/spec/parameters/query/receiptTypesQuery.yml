name: receiptType
in: query
description: |
  Filter by receipt type code.
  To filter by multiple receipt types, repeat the query parameter, for example: `receiptType=8515&receiptType=20803`.
style: form
explode: true
schema:
  type: array
  items:
    $ref: "../../plugins/receipt/schemas/ReceiptTypeEnum.yml"
