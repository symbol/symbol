type: object
description: Block information including an internal resource identifier, block metadata, and the full block header.
required:
  - id
  - meta
  - block
properties:
  id:
    $ref: "../../../schemas/ObjectId.yml"
  meta:
    $ref: "./BlockMetaDTO.yml"
  block:
    anyOf:
      - $ref: "./BlockDTO.yml"
      - $ref: "./ImportanceBlockDTO.yml"
