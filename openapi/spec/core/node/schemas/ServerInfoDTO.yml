type: object
description: |
  Version and deployment information for the catapult-rest component running
  on this node.
required:
  - serverInfo
properties:
  serverInfo:
    $ref: "./ServerDTO.yml"
