type: object
description: |
  REST component version and deployment metadata.
required:
  - restVersion
  - deployment
properties:
  restVersion:
    type: string
    examples:
      - 1.0.14
    description: catapult-rest component version.
  deployment:
    $ref: "./DeploymentDTO.yml"
