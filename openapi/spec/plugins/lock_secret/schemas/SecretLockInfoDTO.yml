type: object
description: Wrapper object containing one secret lock entry and its identifier.
required:
  - id
  - lock
properties:
  id:
    $ref: "../../../schemas/ObjectId.yml"
  lock:
    $ref: "./SecretLockEntryDTO.yml"
