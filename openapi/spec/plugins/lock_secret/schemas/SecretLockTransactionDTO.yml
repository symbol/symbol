type: object
description: Transaction to sends mosaics to a recipient if the proof used is revealed.
  If the duration is reached, the locked funds go back to the sender of the transaction.
allOf:
  - $ref: "../../../core/transaction/schemas/TransactionDTO.yml"
  - $ref: "./SecretLockTransactionBodyDTO.yml"
