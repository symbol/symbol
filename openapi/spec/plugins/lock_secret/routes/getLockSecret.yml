tags:
  - Lock secret routes
summary: Get secret lock information
description: Gets the secret lock for a given secret.
operationId: getLockSecret
parameters:
  - $ref: "../../../parameters/path/secret.yml"
responses:
  "200":
    description: success
    content:
      application/json:
        schema:
          $ref: "../schemas/SecretLockInfoDTO.yml"
  "409":
    $ref : "../../../responses/InvalidArgument.yml"
