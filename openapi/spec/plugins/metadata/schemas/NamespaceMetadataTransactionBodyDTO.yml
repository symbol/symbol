type: object
required:
  - targetPublicKey
  - scopedMetadataKey
  - targetMosaicId
  - valueSizeDelta
  - valueSize
  - value
properties:
  targetPublicKey:
    $ref: "../../../types/PublicKey.yml"
  scopedMetadataKey:
    $ref: "../../../types/MetadataKey.yml"
  targetNamespaceId:
    $ref: "../../../types/NamespaceId.yml"
  valueSizeDelta:
    type: integer
    format: int32
    description: Change in value size in bytes.
  valueSize:
    type: integer
    format: int32
    description: Value size in bytes.
  value:
    type: string
    format: hex
    description: When there is an existing value, the new value is calculated as xor(previous-value, value).
