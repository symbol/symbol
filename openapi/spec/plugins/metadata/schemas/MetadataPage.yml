type: object
required:
  - data
  - pagination
properties:
  data:
    type: array
    description: Array of metadata entries.
    items:
      $ref: "./MetadataInfoDTO.yml"
  pagination:
    $ref: "../../../schemas/Pagination.yml"
