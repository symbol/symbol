type: object
required:
  - address
  - names
properties:
  address:
    $ref: "../../../schemas/Address.yml"
  names:
    type: array
    description: Account linked namespace names.
    example:
      - alias1
      - alias2
    items:
      type: string
