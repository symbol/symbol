type: object
required:
  - mosaicId
  - names
properties:
  mosaicId:
    $ref: "../../../schemas/MosaicId.yml"
  names:
    type: array
    description: Mosaic linked namespace names.
    example:
      - cat.currency
    items:
      type: string
