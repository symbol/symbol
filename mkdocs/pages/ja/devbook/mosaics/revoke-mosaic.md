---
title: モザイクの回収（リボーク）
---

# モザイクの回収（リボーク）

`revokable`（回収可能）フラグを設定して作成された [モザイク] (default: モザイク) は、作成者が任意のアカウントからユニットを回収し、自身の [アカウント] (default: アカウント) 残高に戻すことができます [cite: 1, 4, 7, 72]。これは、契約条項の執行、未使用トークンの回収、または誤った配布の修正に役立ちます [cite: 72]。

このチュートリアルでは、他のアカウントからモザイクユニットを回収する方法を説明します [cite: 72]。

## 前提条件

開始する前に、以下を確認してください。

* 開発環境をセットアップしていること [cite: 72]。
    [開発環境のセットアップ](../start/setup.md) を参照してください [cite: 72]。
* `revokable` フラグが設定されたモザイクを所有する [アカウント] (default: アカウント) を持っていること [cite: 72]。
    [モザイクの作成](./create-mosaic.md) チュートリアルを参照してください [cite: 72]。
* 他のアカウントにいくつかのモザイクユニットを転送済みであること [cite: 72]。
    [転送トランザクション](../transactions/transfer.md) チュートリアルを参照してください [cite: 72]。
* トランザクション手数料を支払うための [XYM] (default: XYM) を入手していること [cite: 72]。
    [蛇口 (Faucet) からテストネットの資金を入手する](../accounts/testnet-faucet.md) を参照してください [cite: 72]。

さらに、トランザクションがどのようにアナウンスされ承認されるかを理解するために、[転送トランザクション](../transactions/transfer.md) チュートリアルを復習しておいてください [cite: 72]。

回収可能性の詳細については、テキストブックの [回収可能性](../../textbook/mosaics.md#回収可能性) を参照してください [cite: 72]。

## 完全なコード

{% import 'tutorial.jinja2' as tutorial with context %}

{{ tutorial.code_full('devbook/mosaics/revoke-mosaic', ['py', 'js']) }}

## コード解説

### アカウントの設定

{{ tutorial.code_snippet(['py:25:40', 'js:21:40']) }}

このスニペットは、署名者の [秘密鍵] (default: 秘密鍵) を `SIGNER_PRIVATE_KEY` 環境変数から読み取ります。設定されていない場合はテストキーがデフォルトとして使用されます [cite: 72]。
署名者の [アドレス] (default: アドレス) は [公開鍵] (default: 公開鍵) から派生します [cite: 72]。
このアカウントは、 `revokable` フラグを持つモザイクの元の作成者である必要があります [cite: 72]。

`SOURCE_ADDRESS` 環境変数は、モザイクユニットが回収されるアカウントのアドレスを指定します [cite: 72]。

`MOSAIC_ID` 環境変数は、回収するモザイクの16進数識別子を指定します [cite: 72]。
アカウントが保持しているモザイクを一覧表示するには、[アカウント残高の照会](../accounts/query-balance.md) を参照してください [cite: 72]。

### ネットワーク時間と手数料の取得

{{ tutorial.code_snippet(['py:43:61', 'js:43:61']) }}

[転送トランザクション](../transactions/transfer.md) チュートリアルで説明されているプロセスに従い、ネットワーク時間と推奨手数料をそれぞれ <get:/node/time> および <get:/network/fees/transaction> から取得します [cite: 72]。

### 初期残高の確認

{{ tutorial.code_snippet(['py:65:69', 'js:65:70']) }}

回収を行う前に、ヘルパー関数 `get_account_mosaics` が <get:/accounts/{accountId}> エンドポイントからソースアカウントの対象モザイクの現在残高を取得します [cite: 72]。
これにより、回収後の結果と比較するための基準が得られます [cite: 72]。

### 回収トランザクションの構築

{{ tutorial.code_snippet(['py:74:84', 'js:75:85']) }}

回収トランザクションは、ソースアカウントからモザイクユニットを回収し、作成者の残高に戻します [cite: 72]。

* **Type:** モザイク供給回収トランザクションにはタイプ `mosaic_supply_revocation_transaction_v1` を使用します [cite: 72]。

* **ソースアドレス:** 回収するモザイクユニットを保持しているアカウントのアドレス [cite: 72]。
    これは、指定されたモザイクのユニットを現在保持している任意のアカウントを指定できます [cite: 72]。

* **モザイク:** [モザイクID] (default: モザイクID) と回収する量を含むオブジェクト [cite: 72]。

* **数量:** ソースアカウントから回収する絶対単位の数 [cite: 72]。
    モザイクの [可分性](../../textbook/mosaics.md#可分性) を確認するには、 <get:/mosaics/{mosaicId}> エンドポイントを照会してください [cite: 72]。
    例えば可分性が `2` の場合、数量 `700` は `7.00` 整数単位（700 / $10^2$）を表します [cite: 72]。

!!! note "メモ" "部分的な回収"

    数量はソースアカウントの全残高と一致する必要はありません [cite: 72]。
    単一のトランザクションで、ソースの現在の保有量までの任意の数量を回収できます [cite: 72]。

### 回収の送信

{{ tutorial.code_snippet(['py:86:106', 'js:87:104']) }}

回収トランザクションは、[転送トランザクションの作成](../transactions/transfer.md#トランザクションのアナウンス) と同じプロセスに従って署名され、アナウンスされます [cite: 72]。

{{ tutorial.code_snippet(['py:108:125', 'js:106:138']) }}

コードはその後、ステータスが `confirmed` に変わるまで <get:/transactionStatus/{hash}> エンドポイントをポーリングして、トランザクションが承認されるのを待ちます [cite: 72]。

### 回収の検証

{{ tutorial.code_snippet(['py:129:133', 'js:142:147']) }}

回収を検証するために、ヘルパー関数 `get_account_mosaics` がソースアカウントの残高を再度取得します [cite: 72]。
残高は [初期残高](#初期残高の確認) よりも回収量分だけ少なくなっているはずです [cite: 72]。

## 出力

以下に示す出力は、プログラムの典型的な実行結果に対応しています [cite: 72]。

```text linenums="1" hl_lines="4 12 24 26 27 47"
--8<-- 'devbook/mosaics/revoke-mosaic.log'
```

出力の主なポイント:

* **モザイクID** (4行目): モザイクID `8857803461494335809` (`0x7aed3d514c986941`) は、回収対象のモザイクを識別します [cite: 72]。

* **初期残高** (12行目): 回収前、ソースアカウントはそのモザイクを `1000` 絶対単位保持しています [cite: 72]。

* **ソースアドレス** (24行目): `source_address` フィールドは、ユニットが回収されるアカウントを識別します [cite: 72]。
    これは、3行目に示されている Base32 [アドレス] (default: アドレス) の16進数エンコード形式です [cite: 72]。

* **回収量** (26-27行目): `mosaic` オブジェクトは、10進数形式のモザイクIDと数量 `700` を指定しています [cite: 72]。
    10進数の値は、4行目に示されている16進数IDに対応します [cite: 72]。

* **検証された残高** (47行目): 回収後、ソースアカウントの残高は `300` になっており、 `700` 絶対単位が正常に回収されたことが確認されました [cite: 72]。

出力に印刷されたトランザクション [ハッシュ] (default: ハッシュ) を使用して、 [Symbol Testnet Explorer](https://testnet.symbol.fyi/) でトランザクションを検索できます [cite: 72]。



## 結論

このチュートリアルでは、以下の方法を説明しました。

| ステップ | 関連ドキュメント |
| -------------------------------------------------------------------------- | ------------------------------------ |
| [アカウント残高を確認する](#初期残高の確認) | <get:/accounts/{accountId}> |
| [モザイクユニットを回収する](#回収トランザクションの構築) | <dy:SymbolTransactionFactory.create> |
| [回収を検証する](#回収の検証) | <get:/accounts/{accountId}> |
