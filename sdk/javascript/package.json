{
  "name": "symbol-sdk-javascript",
  "version": "1.0.0",
  "description": "JavaScript SDK for Symbol",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "mocha --full-trace --recursive ./test",
    "vectors": "node ./vectors/all.js --vectors $(git rev-parse --show-toplevel)/tests/vectors/${BLOCKCHAIN}/crypto --blockchain ${BLOCKCHAIN}",
    "lint:jenkins": "eslint -o lint.sdk.javascript.xml -f junit . || exit 0",
    "test:jenkins": "JUNIT_REPORT_PATH=tests.sdk.javascript.xml mocha --reporter mocha-jenkins-reporter --forbid-only --full-trace --recursive ./test || exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.6",
    "eslint": "^8.7.0",
    "eslint-config-airbnb": "^19.0.4",
    "mocha": "^9.2.0",
    "mocha-jenkins-reporter": "^0.4.7",
    "yargs": "^17.3.1"
  },
  "dependencies": {
    "bitcore-mnemonic": "^8.25.25",
    "js-sha3": "^0.8.0",
    "ripemd160": "^2.0.2",
    "tweetnacl": "^1.0.3"
  }
}
