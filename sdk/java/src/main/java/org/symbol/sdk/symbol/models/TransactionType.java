// Auto-generated by generator. Do not edit directly.
// Run scripts/run_catbuffer_generator.sh to regenerate.

package org.symbol.sdk.symbol.models;

import java.nio.ByteBuffer;
import java.util.Collections;
import java.util.HashMap;
import java.util.Locale;
import java.util.Map;

import org.symbol.sdk.Serializer;
import org.symbol.sdk.utils.BufferView;
import org.symbol.sdk.utils.Converter;

public enum TransactionType implements Serializer {
	ACCOUNT_KEY_LINK(16716),
	NODE_KEY_LINK(16972),
	AGGREGATE_COMPLETE(16705),
	AGGREGATE_BONDED(16961),
	VOTING_KEY_LINK(16707),
	VRF_KEY_LINK(16963),
	HASH_LOCK(16712),
	SECRET_LOCK(16722),
	SECRET_PROOF(16978),
	ACCOUNT_METADATA(16708),
	MOSAIC_METADATA(16964),
	NAMESPACE_METADATA(17220),
	MOSAIC_DEFINITION(16717),
	MOSAIC_SUPPLY_CHANGE(16973),
	MOSAIC_SUPPLY_REVOCATION(17229),
	MULTISIG_ACCOUNT_MODIFICATION(16725),
	ADDRESS_ALIAS(16974),
	MOSAIC_ALIAS(17230),
	NAMESPACE_REGISTRATION(16718),
	ACCOUNT_ADDRESS_RESTRICTION(16720),
	ACCOUNT_MOSAIC_RESTRICTION(16976),
	ACCOUNT_OPERATION_RESTRICTION(17232),
	MOSAIC_ADDRESS_RESTRICTION(16977),
	MOSAIC_GLOBAL_RESTRICTION(16721),
	TRANSFER(16724);

	private final int value;

	private static final Map<Integer, TransactionType> VALUE_MAP;
	static {
		final Map<Integer, TransactionType> map = new HashMap<>();
		for (TransactionType candidate : values())
			map.put(candidate.value, candidate);
		VALUE_MAP = Collections.unmodifiableMap(map);
	}

	private TransactionType(final int value) {
		this.value = value;
	}

	/**
	 * Gets the value field.
	 *
	 * @return The value field.
	 */
	public int getValue() {
		return this.value;
	}

	/**
	 * Creates an instance from its underlying numeric value.
	 *
	 * @param value Value.
	 * @return Instance corresponding to the numeric value.
	 */
	public static TransactionType fromValue(final int value) {
		final TransactionType result = VALUE_MAP.get(value);
		if (null == result)
			throw new IllegalArgumentException("invalid enum value " + value);

		return result;
	}

	/**
	 * Parses a raw value into an instance.
	 *
	 * @param rawValue Raw value to parse.
	 * @return Parsed instance.
	 */
	public static TransactionType parse(final Object rawValue) {
		if (rawValue instanceof TransactionType typed)
			return typed;

		if (rawValue instanceof String name) {
			try {
				return valueOf(name.toUpperCase(Locale.ROOT));
			} catch (final IllegalArgumentException ex) {
				throw new IllegalArgumentException("unknown value " + name + " for type TransactionType", ex);
			}
		}

		if (rawValue instanceof Number number)
			return fromValue(Converter.toInt(number));

		throw new IllegalArgumentException("cannot parse " + rawValue.getClass().getName() + " into TransactionType");
	}

	/**
	 * Calculates the size of the object.
	 *
	 * @return Size of this object in bytes.
	 */
	@Override
	public int size() {
		return 2;
	}

	/**
	 * Deserializes an instance of this object from a byte payload.
	 *
	 * @param buffer Output writer.
	 * @return Deserialized object.
	 * @throws IndexOutOfBoundsException if the buffer is truncated or a declared length overruns it
	 * @throws IllegalArgumentException if a field carries an invalid value (e.g. a reserved field or unknown discriminator)
	 */
	public static TransactionType deserialize(final ByteBuffer buffer) {
		final int value = (int) (new BufferView(buffer).peekInt(2, false));
		return fromValue(value);
	}

	/**
	 * Serializes this object to bytes.
	 *
	 * @return Serialized bytes.
	 */
	@Override
	public byte[] serialize() {
		return Converter.intToBytes(this.value, 2);
	}

	/**
	 * Returns a string representation of this object.
	 *
	 * @return String representation of this object.
	 */
	@Override
	public String toString() {
		return "TransactionType." + name();
	}

	/**
	 * Converts this object to a JSON-serializable representation.
	 *
	 * @return JSON-serializable representation of this object.
	 */
	public int toJson() {
		return this.value;
	}
}
