// Auto-generated by sdk/java/generator. Do not edit directly.
// Run scripts/run_catbuffer_generator.sh to regenerate.

package org.symbol.sdk.symbol.models;

import java.util.HashMap;
import java.util.Map;
import java.util.function.Function;
import java.util.function.Supplier;

import org.symbol.sdk.utils.BufferView;

public final class BlockFactory {
	private BlockFactory() {
		// no instances
	}

	/**
	 * Creates an instance of the concrete type by its name.
	 *
	 * @param entityName Entity (transaction) name.
	 * @return New instance of the named type.
	 */
	public static Block createByName(final String entityName) {
		final Map<String, Supplier<Block>> mapping = new HashMap<>();
		mapping.put("nemesis_block_v1", NemesisBlockV1::new);
		mapping.put("normal_block_v1", NormalBlockV1::new);
		mapping.put("importance_block_v1", ImportanceBlockV1::new);
		final Supplier<Block> supplier = mapping.get(entityName);
		if (supplier == null)
			throw new IllegalArgumentException("unknown Block type " + entityName);

		return supplier.get();
	}

	private static long toKey(final long value) {
		return value;
	}

	private static long toKey(final long a, final long b) {
		return (a << 32) | (b & 0xFFFFFFFFL);
	}

	/**
	 * Deserializes an instance of this object from a byte payload.
	 *
	 * @param view Buffer view to read from; not advanced (the caller moves its cursor by the returned object's size()).
	 * @return Deserialized object.
	 * @throws IndexOutOfBoundsException if the buffer is truncated or a declared length overruns it
	 * @throws IllegalArgumentException if a field carries an invalid value (e.g. a reserved field or unknown discriminator)
	 */
	public static Block deserialize(final BufferView view) {
		final BufferView discriminatorView = view.snapshot();
		final Block parent = new Block();
		Block.deserializeInto(discriminatorView, parent);

		final Map<Long, Function<BufferView, ? extends Block>> mapping = new HashMap<>();
		mapping.put(toKey(NemesisBlockV1.BLOCK_TYPE.getValue()), NemesisBlockV1::deserialize);
		mapping.put(toKey(NormalBlockV1.BLOCK_TYPE.getValue()), NormalBlockV1::deserialize);
		mapping.put(toKey(ImportanceBlockV1.BLOCK_TYPE.getValue()), ImportanceBlockV1::deserialize);

		final long discriminator = toKey(parent.getType().getValue());
		final Function<BufferView, ? extends Block> factoryFn = mapping.get(discriminator);
		if (factoryFn == null)
			throw new IllegalArgumentException("unknown Block discriminator " + discriminator);

		return factoryFn.apply(view);
	}

	/**
	 * Deserializes an instance of this object from a byte payload.
	 *
	 * @param payload Byte payload to deserialize.
	 * @return Deserialized object.
	 * @throws IndexOutOfBoundsException if the buffer is truncated or a declared length overruns it
	 * @throws IllegalArgumentException if a field carries an invalid value (e.g. a reserved field or unknown discriminator)
	 */
	public static Block deserialize(final byte[] payload) {
		return deserialize(new BufferView(payload));
	}

}
