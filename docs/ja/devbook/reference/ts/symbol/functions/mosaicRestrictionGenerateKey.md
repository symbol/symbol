# Function: mosaicRestrictionGenerateKey()

```ts
function mosaicRestrictionGenerateKey(seed): bigint
```

Generates a mosaic restriction key from a string.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `seed` | `string` | Mosaic restriction key seed. |

## Returns

`bigint`

Mosaic restriction key.
