# Function: metadataGenerateKey()

```ts
function metadataGenerateKey(seed): bigint
```

Generates a metadata key from a string.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `seed` | `string` | Metadata key seed. |

## Returns

`bigint`

Metadata key.
